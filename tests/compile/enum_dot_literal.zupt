--TEST--
a dot enum literal infers its enum from the annotated binding type
--FILE--
enum Color {
  Red,
  Green,
  Blue,
}

pub fn main(): i32 {
  let c: Color = .Blue;
  if c == Color.Blue {
    return c as i32;
  }
  return 0;
}
--EXIT--
2
