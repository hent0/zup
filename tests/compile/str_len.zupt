--TEST--
a string literal carries its length
--FILE--
extern fn printf(fmt: cstr, ...): i32;

pub fn main(): i32 {
  let s: str = "hello";
  printf("%d\n", s.len as i32);
  return 0;
}
--EXPECT--
5
--EXIT--
0
