--TEST--
an int casts to f64 and participates in float arithmetic: 7 as f64 / 2.0 == 3.5
--FILE--
extern fn printf(fmt: cstr, ...): i32;

pub fn main(): i32 {
  printf("%g\n", 7 as f64 / 2.0);
  return 0;
}
--EXPECT--
3.5
--EXIT--
0
