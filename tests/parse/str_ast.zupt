--TEST--
a string literal has type str; .len reads the length field
--ARGS--
--ast
--FILE--
pub fn main(): i32 {
  let s: str = "hi";
  let n: i64 = s.len;
  return 0;
}
--EXPECT--
Container (file) (1 members)
  FnDecl pub 'main' -> i32
    Let s: str
      String literal "hi"
    Let n: i64
      Field len
        Id s
    Return
      Number literal 0
--EXIT--
0
