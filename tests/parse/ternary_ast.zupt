--TEST--
'cond ? a : b' parses as a ternary expression with three children
--ARGS--
--ast
--FILE--
pub fn main(): i32 {
  return true ? 1 : 2;
}
--EXPECT--
Container (file) (1 members)
  FnDecl pub 'main' -> i32
    Return
      Ternary
        Boolean literal true
        Number literal 1
        Number literal 2
--EXIT--
0
