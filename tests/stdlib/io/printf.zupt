--TEST--
a locally declared extern printf passes mixed variadic arguments through the C ABI
--FILE--
extern fn printf(fmt: cstr, ...): i32;

pub fn main(): i32 {
  printf("%d %s %g\n", 7, "hi", 0.5);
  return 0;
}
--EXPECT--
7 hi 0.5
--EXIT--
0
