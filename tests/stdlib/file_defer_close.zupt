--TEST--
the open/defer close idiom: the file handle is released at scope exit and the write lands on disk
--FILE--
const std = import("std");

fn writeIt() {
  const file = std.io.open("/tmp/zup_defer.txt", .Write);
  defer file.close();
  file.writeln("deferred");
}

pub fn main(): i32 {
  writeIt();
  let f: std.io.File = std.io.open("/tmp/zup_defer.txt", .Read);
  defer f.close();
  let buf: [16]u8;
  std.io.println(f.readln(buf) ?? "");
  return 0;
}
--CLEANUP--
rm -f /tmp/zup_defer.txt
--EXPECT--
deferred
--EXIT--
0
