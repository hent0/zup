--TEST--
std.exit terminates the process with the given status code
--FILE--
const std = import("std");

pub fn main(): i32 {
  std.exit(3);
  std.io.printf("unreachable\n");
  return 0;
}
--EXIT--
3
