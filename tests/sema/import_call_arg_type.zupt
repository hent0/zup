--TEST--
arguments to a module-qualified call are type-checked against the imported function's parameters
--FILE--
const std = import("std");

pub fn main(): i32 {
  std.println(5);
  return 0;
}
--EXPECTERR--
zup: error: cannot pass i32 as argument 's' of 'println' (expected str) [4:7]
--EXIT--
1
