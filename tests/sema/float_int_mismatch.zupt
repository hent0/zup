--TEST--
f64 and integer types are distinct; mixing them in arithmetic is an error (no implicit conversion)
--FILE--
fn f(a: f64, b: i32): f64 {
  return a + b;
}

pub fn main(): i32 {
  return 0;
}
--EXPECTERR--
zup: error: cannot apply '+' to f64 and i32 [2:10]
--EXIT--
1
