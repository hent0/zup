--TEST--
floats support arithmetic and comparison; bitwise operators are still rejected on f64
--FILE--
fn f(a: f64, b: f64): f64 {
  return a & b;
}

pub fn main(): i32 {
  return 0;
}
--EXPECTERR--
zup: error: cannot apply '&' to f64 and f64 [2:10]
--EXIT--
1
