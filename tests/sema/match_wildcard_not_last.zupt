--TEST--
the '_' arm must come last; arms after it would be unreachable
--FILE--
enum Color {
  Red,
  Green,
}

pub fn main(): i32 {
  return match Color.Red {
    _ => 0,
    .Red => 1,
  };
}
--EXPECTERR--
zup: error: '_' must be the last match arm [8:5]
--EXIT--
1
