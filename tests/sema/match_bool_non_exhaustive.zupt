--TEST--
a bool match without '_' must cover both true and false
--FILE--
const std = import("std");

pub fn main(): i32 {
  const b = true;
  match b {
    true => std.io.println("ok"),
  };
  return 0;
}
--EXPECTERR--
zup: error: match does not cover 'false' [5:3]
--EXIT--
1
