const std = import("std");

fn fib(n: i32) {
  if n <= 0 {
    return;
  }

  let a: i32 = 0;
  let b: i32 = 1;

  std.io.print("{} ", a);

  if n > 1 {
    std.io.print("{} ", b);
  }

  for i in 3..n + 1 {
    let current: i32 = a + b;
    std.io.print("{} ", current);

    a = b;
    b = current;
  }

  std.io.print("\n");
}

pub fn main(): i32 {
  fib(10);
  return 0;
}
