const std = import("std");

pub fn main(): i32 {
  let input: [64]u8;

  std.io.print("Wait a minute, who are you?\n> ");
  let name: str = std.io.stdin().readln(input);

  std.io.printf("Hello, %s", name);

  return 0;
}
