const std = import("std");

pub fn main(): i32 {
  let input: [64]u8;

  std.io.print("Wait a minute, who are you?\n> ");
  let name: str = match std.io.stdin().readln(input) {
    .Ok(line) => line,
    .Eof => "",
    .Err(e) => "",
  };

  std.io.print("Hello, {}", name);

  return 0;
}
