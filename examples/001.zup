fn number(x: i32): i32 {
  return x;
}

// Single line comment
pub fn /** Stupid comment here */ main(): i32 {
  printf("Hello, %s %d!\n", "world", number(123));
  /**
   * Multiline comment
   */
  return number(123);// Line breaking comment
}
