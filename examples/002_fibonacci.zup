const std = import("std");

fn fib(n: i32, a: i32, b: i32): i32 {
  if n <= 0 {
    return 0;
  }

  std.io.print("{} ", a);
  return fib(n - 1, b, a + b);
}

pub fn main(): i32 {
  fib(10, 0, 1);
  std.io.print("\n");
  return 0;
}
